#Web Settings:
server.port=8080
server.context-path=/stats
logging.level.org.springframework.web=DEBUG
#Data Source:
# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

spring.datasource.url = jdbc:mysql://localhost:3306/alm_nona?useSSL=false

# Username and password
spring.datasource.username = jose
spring.datasource.password = jose

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

#Hibernate:
spring.jpa.hibernate.ddl-auto = false
spring.jpa.generate-ddl = false
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#Mail configuration:
mail.username = aplikacjakontakty@gmail.com
mail.password = QwErTy1234

#Mail data:
mail.message.confirm.template = Link do aplikacji: \r\n\r\n{0}
mail.message.password.template = Nowe has\u0142o\: {0}